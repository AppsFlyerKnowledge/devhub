# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-21T10:43:21.052Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Master API

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/analytics/master_api.yaml --key=${{ secrets.README_API_KEY }} --branch=0.1 --legacy-id=63d267e835fb4100501bba5a --confirm-overwrite
