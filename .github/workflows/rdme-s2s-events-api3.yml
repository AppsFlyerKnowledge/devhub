# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-12-10T15:12:32.780Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `rdme-s2s-events-api3`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/rta/s2s_events_api3.yaml --key=${{ secrets.README_API_KEY }} --slug=server-to-server-events-api-for-mobile.json --confirm-overwrite
