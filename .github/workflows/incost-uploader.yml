# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-01-11T09:01:40.750Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: InCost API

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./docs/apis/ROI360/incost_uploader.yaml --key=${{ secrets.README_API_KEY }} --id=659fad5eb597bf0045b7c4ce
      
      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs ./docs/apis/ROI360/incost_overview.md --key=${{ secrets.README_API_KEY }} --version=0.1
