# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-07-05T08:24:50.666Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Skan postbacks by date API

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./docs/apis/SKAD/skan_postbacks_by_date_api.yaml --key=${{ secrets.README_API_KEY }} --id=64a528803f0a300a33e8602d
