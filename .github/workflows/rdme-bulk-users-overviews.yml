# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-01-28T08:34:24.404Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Create bulk users API overview markdown

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `docs` overview command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs ./docs/apis/management/bulk_users_management_overview.md --key=${{ secrets.README_API_KEY }} --version=0.1
      
      - name: Run `docs` errors command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs ./docs/apis/management/bulk_users_management_errors.md --key=${{ secrets.README_API_KEY }} --version=0.1

      - name: Run `docs` formats command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs ./docs/apis/management/bulk_users_management_formats.md --key=${{ secrets.README_API_KEY }} --version=0.1

