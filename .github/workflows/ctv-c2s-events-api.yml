# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-07-09T19:41:25.329Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: CTV C2S Events API 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./rta/ctv_c2s_events_api.yaml --key=${{ secrets.README_API_KEY }} --id=64ab0d5419d95b00eadb1b71
