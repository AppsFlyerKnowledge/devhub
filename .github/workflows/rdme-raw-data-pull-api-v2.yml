# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-21T09:27:13.845Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Raw-Data-Pull-API-v2

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/mos/raw_data_pull_api_tokenv2.yaml --key=${{ secrets.README_API_KEY }} --slug=raw-data-pull-api-v2-token.json --confirm-overwrite
