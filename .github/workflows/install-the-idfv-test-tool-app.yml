# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-08-15T09:22:52.524Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Install the idfv test tool app
on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: docs upload ./docs/guides/auxiliary_tools/ --key=${{ secrets.README_API_KEY }} --branch=0.1 --confirm-overwrite
