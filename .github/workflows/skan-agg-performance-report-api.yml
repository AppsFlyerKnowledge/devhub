# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-07-06T11:15:38.894Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: SKAN performance report API

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/SKAD/skan_agg_performance_report_api.yaml --key=${{ secrets.README_API_KEY }} --slug=skan-aggregated-performance-report-api.json --confirm-overwrite
