# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-21T09:25:01.987Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: OneLink API

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/OneLink/onelinkapi.yml --key=${{ secrets.README_API_KEY }} --legacy-id=624693c6fa631302dde62cf4 --confirm-overwrite
      
      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: docs upload ./docs/apis/OneLink/onelinkapi_overview.md --key=${{ secrets.README_API_KEY }} --confirm-overwrite
