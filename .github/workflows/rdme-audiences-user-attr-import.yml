# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-22T07:32:06.668Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Audiences User Attribute Import 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/audience/audiences_user_attr_import.yaml --key=${{ secrets.README_API_KEY }} --slug=audiences-user-attribution-import-api.json --confirm-overwrite
