# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-21T10:42:47.672Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ROI360-True-Tax-API

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./docs/apis/ROI360/true_revenue_api.yaml --key=${{ secrets.README_API_KEY }} --id=63eb8607ce3ff30366cc2b8c
