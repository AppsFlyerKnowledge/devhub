# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2025-05-18T06:37:29.181Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: OneLink API v2

# Workflow disabled - uncomment the 'push:' section below to re-enable automatic runs
on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v4

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v9
        with:
          rdme: openapi ./docs/apis/OneLink/onelinkapi_v2.yml --key=${{ secrets.README_API_KEY }} --id=6829800b8e61c5005284ad12
      
      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v9
        with:
          rdme: docs ./docs/apis/OneLink/onelinkapi_v2_overview.md --key=${{ secrets.README_API_KEY }} 

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v9
        with:
          rdme: docs ./docs/apis/OneLink/migrate_to_onelink_api_v2.md --key=${{ secrets.README_API_KEY }} 
