# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-21T09:29:07.057Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Aggregate Pull API v1

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/analytics/aggregate_pull_api_v1.yaml --key=${{ secrets.README_API_KEY }} --slug=aggregate-pull-api-v1-token.json --confirm-overwrite
