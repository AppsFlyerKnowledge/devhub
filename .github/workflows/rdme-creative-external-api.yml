# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2026-06-17T00:00:00.000Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Creative External API

# Workflow disabled - uncomment the 'push:' section below to re-enable automatic runs
on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v4

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/creative/creative_external_api.yaml --key=${{ secrets.README_API_KEY }} --slug=creative-external-api.json --confirm-overwrite

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: reference upload ./docs/apis/creative/creative_external_api_overview.md --key=${{ secrets.README_API_KEY }}
