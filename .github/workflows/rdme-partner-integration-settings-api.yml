# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-21T09:32:20.557Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Partner Settings Integration API

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/partnerIntSet/partner_integration_settings_api.yaml --key=${{ secrets.README_API_KEY }} --legacy-id=615eb7b60319170356179796 --confirm-overwrite
