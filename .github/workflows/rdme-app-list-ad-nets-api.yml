# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-06-27T12:55:59.719Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v10
        with:
          rdme: openapi upload ./docs/apis/management/app_list_ad_nets_api.yaml --key=${{ secrets.README_API_KEY }} --branch=0.1 --legacy-id=649adc4b66993d000bd3b722 --confirm-overwrite
